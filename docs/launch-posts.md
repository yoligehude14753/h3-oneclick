# Launch Posts — h3-oneclick

仓库公开后各平台的发布文案。原则：X 视频/图片原生上传（外链被降权）；每个渠道单独改写法，不要同一份文案同时群发；发后全天蹲守回复。

发布顺序建议：周二~周四 → Show HN（早 8-9 点 PT）+ X thread 同上午 → Reddit 各版分开时段 → 中文圈（即刻/V2EX）。

前置条件：仓库转 public 前补 `LICENSE` 文件 + 换掉 README 里的 `license: private` badge。

---

## 1. X / Twitter — 主推 thread（英文）

**Tweet 1（pin，配原生上传的 mp4/GIF）**

> Setting up ComfyUI + a video model on a bare GPU box takes hours: env, weights, LoRA, workflow JSON, custom nodes.
>
> I built a single binary that does all of it — detects your GPU, picks the right profile, downloads everything, and hands you back a generated video.
>
> 40s of it running on a 2× RTX 5090D box 🧵

**Tweet 2**

> It probes hardware → gates profiles by free VRAM & platform → reuses existing ComfyUI + weights instead of re-downloading 25GB → falls back to mirrors when HF times out → writes receipts for every step.

**Tweet 3**

> No mock: the clip at the end of the demo is the actual output — 864×480, 5.2s, h264+AAC, generated by the ComfyUI it just installed.
>
> Go single-binary · desktop GUI (Wails) or web UI · Win/Linux/macOS
> github.com/yoligehude14753/h3-oneclick

## 2. X — 中文短推

> 裸机跑 H3 视频生成要配多久？我写了个单文件工具：自动探测 GPU/显存 → 按档位选 profile → 下模型/LoRA/workflow → 起 ComfyUI → 直接吐出 mp4。双 5090D 实测 40 秒从安装到出片。开源了👇
>
> [配 GIF/mp4 + 链接]

## 3. Hacker News — Show HN

**标题：**

> Show HN: H3 OneClick – one binary that turns a bare GPU box into an H3 video-gen node

**正文（maker comment，发布后立刻贴）：**

> Detects hardware (GPU/VRAM/RAM/disk), discovers existing ComfyUI installs, gates profiles by free VRAM, downloads weights/LoRA/workflow with mirror fallback, then actually generates a video. Receipts for every step. Go, single binary, desktop GUI + web UI. Verified on 2× RTX 5090D — the demo video ends with the real generated mp4.
>
> Honest limits: Windows CUDA path untested, default instance scan misses non-standard ComfyUI paths (workaround: explicit paths in /api/scan).

## 4. Reddit — r/comfyui / r/LocalLLaMA（problem-first，不要营销腔）

**标题：**

> I got tired of hand-configuring ComfyUI for video models, so I wrote a one-binary installer that ends with an actual generated video

**正文要点：**

- 先讲真实痛点：权重路径、LoRA 版本对不上、官方 workflow 是 subgraph UI 格式不能直接 POST /prompt、HF 超时
- GIF 放中间
- 结尾坦诚边界（Windows 未实测、BUG-002 扫描路径问题）——Reddit 吃诚实不吃营销

## 5. 中文圈 — 即刻 / V2EX

**标题方向：**「给 5090 装 H3 视频生成环境，从装 Python 到出片我踩了多少坑」

**结构：**

1. 踩坑叙事开头：GitHub raw 超时 → jsDelivr 兜底；官方 workflow subgraph 格式 → 写转换器；显存门控怎么定
2. 中间放 GIF
3. 结尾：仓库链接 + 「40 秒从裸机到出片」结果

V2EX 吃技术故事，即刻吃结果数字，同一份内容两个标题。
